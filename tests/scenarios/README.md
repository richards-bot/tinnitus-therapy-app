# Scenario Tests

Use this folder for scenario-based verification of critical paths.

These tests should answer questions like:

- can a real user complete the primary flow?
- does the system fail safely when an expected dependency breaks?
- does the UI/backend behave correctly under realistic conditions?

Start with `docs/templates/scenario-test.md` when designing a scenario.
